# テーマルートで

npm install

# または明示的に入れる場合

npm install -D vite sass postcss postcss-cli autoprefixer cssnano

# php を監視

npm install -D vite-plugin-full-reload

#　 tailwindo を入れる
npm install -D tailwindcss @tailwindcss/cli postcss autoprefixer
npx @tailwindcss/cli -v
